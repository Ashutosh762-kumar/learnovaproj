# Learnova Backend (Complete)

Starter backend for Learnova using Node.js, Express and MongoDB.

## Setup
1. Copy `.env.example` -> `.env` and fill values.
2. npm install
3. npm run dev

API endpoints:
- /api/auth
- /api/users
- /api/courses
- /api/quizzes
- /api/progress
- /api/enrollments
